# Shared dependencies
python-dotenv>=1.0.0  # held back at upgrade-deps time: aider-chat (apps/worker) hard-pins python-dotenv to an exact version (<=1.2.1 as of aider-chat 0.86.2); kept in lockstep with the other requirements.txt files so `pip install -r apps/shared/requirements.txt -r apps/orchestrator/requirements.txt -r apps/worker/requirements.txt` still resolves
PyGithub>=2.10.0
pytz>=2024.2
requests>=2.31.0  # held back at upgrade-deps time: aider-chat (apps/worker) hard-pins requests to an exact version (<=2.32.5 as of aider-chat 0.86.2); kept in lockstep with the other requirements.txt files so `pip install -r apps/shared/requirements.txt -r apps/orchestrator/requirements.txt -r apps/worker/requirements.txt` still resolves
redis>=5.3.1
