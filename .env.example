# API Keys
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# GitHub Token (OPTIONAL - for orchestrator read-only operations)
# If not provided, will use first dog's token as fallback
# Get from: https://github.com/settings/tokens (Fine-grained tokens tab)
# Repository permissions needed (if using separate token):
#   - Contents: Read-only
#   - Pull requests: Read-only
#   - Metadata: Read-only (auto-selected)
# Token format: github_pat_... (fine-grained) or ghp_... (classic - deprecated)
# Note: Each dog has its own token in the DOGS array below (required)
# GITHUB_TOKEN=ghp_...

# Slack Bot Tokens
# Get from: https://api.slack.com/apps -> Your App -> OAuth & Permissions
SLACK_BOT_TOKEN=xoxb-...
# Get from: https://api.slack.com/apps -> Your App -> Basic Information -> App-Level Tokens
SLACK_APP_TOKEN=xapp-...

# Repository Configuration
# Format: owner/repo (e.g., "bryans-org/myproject")
GITHUB_REPO=username/reponame

# Slack Configuration
# Get channel ID: right-click channel -> View channel details -> copy ID at bottom
SLACK_CHANNEL_ID=C123456

# Redis Configuration
# Local development: redis://localhost:6379
# Railway: will be provided automatically via $REDIS_URL
REDIS_URL=redis://localhost:6379

# Dog Configuration (Multiple Dogs with per-dog GitHub tokens)
# JSON array of dog agents - each dog needs a unique GitHub account and PAT
# Format: [{"name": "GitHub-Username", "email": "email@example.com", "github_token": "github_pat_..."}, ...]
# Each dog's github_token must have:
#   - Contents: Read and write
#   - Pull requests: Read and write
#   - Metadata: Read-only (auto-selected)
# You can configure 1 to N dogs for parallel task processing
DOGS='[
  {"name": "Bryans-Coregi", "email": "coregi@bryanowens.dev", "github_token": "github_pat_11AAA..."},
  {"name": "Bryans-Bitbull", "email": "bitbull@bryanowens.dev", "github_token": "github_pat_11BBB..."},
  {"name": "Bryans-Poodle", "email": "poodle@bryanowens.dev", "github_token": "github_pat_11CCC..."}
]'

# Legacy: Single Dog Configuration (DEPRECATED - use DOGS instead)
# If DOGS is not set, falls back to DOG_NAME/DOG_EMAIL/DOG_GITHUB_TOKEN for backward compatibility
# DOG_NAME=Coregi
# DOG_EMAIL=coregi@example.com
# DOG_GITHUB_TOKEN=github_pat_...

# Optional: Git Configuration
# Default branch to create PRs against
BASE_BRANCH=main
